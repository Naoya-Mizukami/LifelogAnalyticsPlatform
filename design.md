# LifeLog Analytics Platform 設計書

## 0.1 背景と目的

現在参画している現場では、データ分析基盤の構築・運用が求められているが、
これまでの自身の経験は運用業務が中心であり、
SQLを用いた設計・集計・データマート構築の実装経験が不足しているという課題があった。

今後もデータ分析基盤案件に継続的に参画するためには、
単なる運用対応ではなく、
「データをどのように設計し、どのように集計・提供するか」を
自ら考え、実装した経験を持つ必要があると考えた。

本システムでは、実生活で発生する複数の生活ログ（家計簿・学習・体重など）を対象に、
データの取り込みから集計・分析用データマート作成までを一貫して行うデータ分析基盤を構築する。

これにより、以下を目的とする。

- SQLを用いた集計用中間テーブルおよびデータマート設計・実装経験の獲得
- Pythonによるバッチ処理開発経験の獲得
- データ分析基盤の設計〜実装〜検証までを一通り自走できる状態になること


## 0.2 対象範囲（スコープ）

本システムでは、以下の生活ログを収集・分析対象とする。

- 家計簿ログ（クレジットカード明細CSV）
- 学習ログ（手入力CSV）
- 体重ログ（手入力CSV）

また、本システムでは、以下は対象外とする。

- Webアプリケーションの開発（画面・UIの作成）
- クレジットカードや銀行アプリへの自動ログイン・スクレイピング
- リアルタイム処理やストリーミング処理

本システムはあくまで、
「データ分析基盤の設計・構築」を目的とするため、
データ取得はCSVファイルの手動投入を前提とする。


## 0.3 技術選定

本システムでは、以下の技術を使用する。

- データベース：PostgreSQL  
  - 分析基盤案件での利用実績が多く、SQL設計・集計の練習に適しているため

- プログラミング言語：Python  
  - CSV取り込みやバッチ処理実装に適しているため

- クエリ言語：SQL  
  - 集計用中間テーブル、データマート構築を目的として使用

- コンテナ：Docker  
  - ローカル環境構築を簡易化するため

- バージョン管理：Git  
  - 設計・実装の変更履歴を管理するため

- データマートプラットフォーム：dbt (余裕があれば)
  - データの加工とデータマートの管理の簡易化のため


## 1. 要件定義

### 1.1 機能要件（できること）

#### データ取り込み
- クレジットカード明細CSVを指定フォルダに置くと、DBに取り込める
- 学習ログCSV・体重ログCSVを指定フォルダに置くと、DBに取り込める
- 取り込み時に、入力ファイル名・取り込み日時などの情報を記録できる
- 同じCSVを再度取り込んでも、データが二重登録されない（再実行できる）

#### データ整形（使いやすくする）
- 取り込んだ生データから、日付や金額などを集計しやすい形に整える
- クレカ明細の「利用日/計上日」など複数日付がある場合でも、集計に使う日付を決めて扱える
- 金額の表記ゆれ（マイナス/プラスなど）があっても、支出として統一した形式で扱える

#### 集計（分析用の数字を作る）
- 日ごとの支出合計／学習時間合計／体重を計算できる
- 週ごとの支出合計／学習時間合計／体重を計算できる
- 月ごとの支出合計／学習時間合計／体重を計算できる
- カテゴリ別の支出合計／学習時間合計を計算できる  
  （支出は費目カテゴリ、学習は科目・分野などの分類を想定）
- 生活ログ（支出・学習・体重）を日付でまとめた一覧データを作れる
- 初期段階では「カテゴリ分類なし」でも集計できる  
  （合計値・時系列推移の把握を優先する）

#### 実行・運用
- 取り込み〜集計までを、コマンド1つで一括実行できる
- 処理が成功したか失敗したかが分かる（ログが残る）

#### 補足（スコープ上の立ち位置）
- 本システムは「画面を作ること」ではなく、「分析用データを安定して作ること」を目的とする

#### 追加要件（余裕があれば実施）
- 店名（摘要）を元にカテゴリ分類を行うためのカテゴリマスタ（対応表）を作成し、集計に反映できる


### 1.2 非機能要件（壊れにくさ・運用しやすさの条件）

#### 再実行性（同じ処理を何回やっても壊れないこと）
- 同じCSVファイルを再度取り込んでも、データが二重登録されない
- 取り込み処理はトランザクション管理されており、途中でエラーが発生した場合は全体をロールバックする
- エラー内容を修正した後、同じ処理を再実行できる

#### データ検証とエラー時の挙動
- 入力データに不正な値（例：金額の形式不正、日付形式不正）が含まれている場合、処理を中断する
- 不正が検知された場合は、どの行でどのような不正が発生したかをログに出力する
- 不正データは自動補正せず、入力データの修正を促す方針とする

#### ログ出力（処理結果が後から追えること）
- 処理対象となったファイル名をログに出力する
- CSVの読み込み行数、DBへの登録件数をログに出力する
- エラー発生時は、エラー内容およびロールバック実行をログに出力する
- ログは後から処理結果を確認できる形式で保存される

#### 処理性能
- 日次・週次での運用を想定し、1回の処理時間は目安として10秒〜1分以内とする
- データ量の増加により処理時間が増加した場合は、SQLの改善やインデックス追加により対応できる設計とする

#### データ品質の扱い
- NULLや空欄が発生しうる項目については、事前に扱い方を定義した上で処理する
  - 例：体脂肪率は未入力でも処理を継続する
- 集計に影響する項目（日付・金額など）は必須項目として扱う

#### 環境構築・再現性
- PostgreSQLはDockerを用いて起動できる
- 初回セットアップから実行までの手順はREADMEに明記する
- 他の環境でも同様に再現可能な構成とする


## 2. 設計
### 2.1 アーキテクチャ設計（全体処理フロー）

本システムは、生活ログ（家計簿・学習・体重）を対象に、
データの取り込みから集計用データマート作成までを
バッチ処理によって一括で実行するデータ分析基盤である。

処理は大きく以下の3段階で構成される。

#### ① データ取り込み（raw層）

利用者が指定フォルダに配置したCSVファイルを入力として、
Pythonバッチによりデータベースへ取り込む。

取り込み時は、CSVの内容を極力加工せず、
生データとしてそのままrawテーブルに保存する。
あわせて、以下のメタ情報を記録する。

- 取り込み元ファイル名
- CSVの行番号
- 取り込み日時

取り込み処理はトランザクション管理されており、
入力データに不正な値が検知された場合は処理を中断し、
rawテーブルへの変更をロールバックする。

#### ② データ整形・正規化（stg層）

rawテーブルに保存された生データをもとに、
集計しやすい形式へ変換したデータをstgテーブルとして作成する。

この段階では、以下の処理を行う。

- 日付形式の統一（集計に使用する日付の決定）
- 金額の数値化および符号の統一
- 不要な項目の除外
- NULLや空欄の扱いの整理

stgテーブルは、以降の集計処理の中間成果物として位置づけ、
生データ（raw）と分析用データ（mart）を分離する役割を持つ。

#### ③ 集計・分析用データ作成（mart層）

stgテーブルを入力として、
分析・可視化用途に利用するデータマート（mart）を作成する。

mart層では、以下の集計データを生成する。

- 日次・週次・月次の支出合計
- 日次・週次・月次の学習時間合計
- 日次・週次・月次の体重推移
- 生活ログ（支出・学習・体重）を日付単位で統合した一覧データ

カテゴリ分類については初期段階では必須とせず、
合計値および時系列推移の把握を優先する。
将来的に必要となった場合は、
カテゴリマスタを追加することで拡張可能な構成とする。

#### ④ 処理実行方式

データ取り込みから集計までの一連の処理は、
Pythonバッチのエントリーポイントを1つ用意し、
コマンド1つで一括実行できる構成とする。

処理の各段階ではログを出力し、
処理対象ファイル、処理件数、エラー内容が後から確認できるようにする。


### 2.2 テーブル設計（テーブル一覧と役割）

本システムでは、データを以下の3層（raw / stg / mart）に分けて管理する。
- raw：CSVの内容をほぼそのまま保存する層（証跡・再現性）
- stg：集計しやすい形に整える層（正規化・中間）
- mart：分析に使う集計結果を保存する層（データマート）

設計方針：
- stgは「日別にまとめず」、明細のまま（1取引＝1行）で保持する  
  → 集計はmartで行う
- 週次の集計は「月曜始まり」を採用する

---

#### 【 raw層 】（生データ）

#### raw_card_statement（クレジットカード明細の生データ）
- 目的：カード会社CSVの内容を極力加工せず保存する（後から再現できるようにする）
- 主な中身：利用日/計上日/摘要（店名）/金額など（文字列のままでも保持）
- 付帯情報：取り込み元ファイル名、CSV行番号、取り込み日時

#### raw_study_log（学習ログの生データ）
- 目的：学習ログCSVの入力をそのまま保存する
- 主な中身：学習日、学習時間（分）、科目（分類）

#### raw_weight（体重ログの生データ）
- 目的：体重ログCSVの入力をそのまま保存する
- 主な中身：計測日、体重、体脂肪率（任意）

---

#### 【 stg層 】（集計しやすい形に整える：明細のまま保持）

#### stg_expense（支出の正規化データ：1取引＝1行）
- 目的：日付・金額・摘要などを集計しやすい形に統一する
- 主な中身：支出日（集計用日付）、金額（支出＝正）、摘要（店名）、ファイル情報
- 備考：カテゴリ分類は初期段階では必須としない（合計・推移を優先）

#### stg_study（学習の正規化データ：1記録＝1行）
- 目的：学習時間を集計しやすい形に統一する
- 主な中身：学習日、学習時間（分）、科目（分類）

#### stg_weight（体重の正規化データ：1記録＝1行）
- 目的：計測値を日付単位で扱える形にする
- 主な中身：計測日、体重、体脂肪率（任意）

---

#### 【 mart層 】（分析用の集計結果）

#### mart_life_daily（日次の統合サマリ）
- 目的：支出・学習・体重を日付単位でまとめた一覧を提供する
- 主な中身：日付、支出合計、学習時間合計、体重（当日値または平均）

#### mart_life_weekly（週次の統合サマリ：月曜始まり）
- 目的：週単位の推移を確認できるようにする
- 主な中身：週開始日（月曜）、支出合計、学習時間合計、体重（週平均など）

#### mart_life_monthly（月次の統合サマリ）
- 目的：月単位の推移を確認できるようにする
- 主な中身：月、支出合計、学習時間合計、体重（月平均など）

---

#### 【（拡張）カテゴリ分類 】（余裕があれば）

#### mst_category_rule（カテゴリ分類ルール）
- 目的：摘要（店名）などを元にカテゴリ分類するための対応表
- 主な中身：キーワード（または正規表現）、カテゴリ名、優先度
- 備考：初期は「カテゴリ無し」で稼働し、必要になったら追加する

#### mart_category_summary（カテゴリ別集計）
- 目的：カテゴリ別の支出合計／学習時間合計を提供する
- 主な中身：日/週/月、カテゴリ、合計値



### 2.3 バッチ設計（処理構成・実行フロー）

本システムでは、生活ログ（家計簿・学習・体重）を対象に、
データの取り込みから集計用データマート作成までを
バッチ処理によって一括で実行する。

設計方針は以下のとおりとする。

- 実行はコマンド1つで完結する
- 入力データに不正があった場合は処理を中断し、DBへの変更はロールバックする
- 処理結果はログとして記録し、後から実行状況を確認できるようにする
- フォルダ内に存在するCSVファイルはまとめて処理する
- raw / stg / mart の中核テーブルは事前にDDLで作成し、バッチ実行時にはDROP/CREATEしない
- データ加工用の中間テーブルは `CREATE TEMP TABLE` を用い、処理実行のたびに生成する
  - 中間テーブルはセッション終了時に自動的に破棄される


---

#### 1. バッチの構成

バッチは以下の構成で実行される。

- エントリーポイント：`run_all`
  - raw取り込み処理
  - stg作成処理
  - mart作成処理

`run_all` を実行することで、取り込みから集計までの一連の処理が順番に実行される。

---

#### 2. 入力ファイルの配置ルール

利用者は、事前に以下のフォルダへCSVファイルを配置する。

- `data/input/card/`  
  クレジットカード明細CSV（複数ファイル可）

- `data/input/study/`  
  学習ログCSV（複数ファイル可）

- `data/input/weight/`  
  体重ログCSV（複数ファイル可）

※ 入力ファイルは個人情報を含むため、Git管理対象外とする。

---

#### 3. 処理フロー

`run_all` 実行時、以下の順序で処理を行う。

#### 【 3.1 raw取り込み処理 】

1. 入力フォルダを走査し、対象となるCSVファイル一覧を取得する
2. 各CSVファイルについて、処理開始時にファイル名をログに出力する
3. CSVを読み込み、行ごとにデータ検証を行う  
   （例：日付形式、金額形式のチェック）
4. 不正なデータが検知された場合は処理を中断し、トランザクションをロールバックする
5. 問題がなければ、rawテーブルへデータを登録する
6. CSVの読み込み行数およびDB登録件数をログに出力する

対象テーブル：
- `raw_card_statement`
- `raw_study_log`
- `raw_weight`

---

#### 【 3.2 stg作成処理（整形） 】

7. rawテーブルを入力として、stgテーブルを作成・更新するSQLを実行する
8. 処理の開始および終了をログに出力する

対象テーブル：
- `stg_expense`（支出：1取引＝1行）
- `stg_study`
- `stg_weight`

---

#### 【 3.3 mart作成処理（集計） 】

9. stgテーブルを入力として、martテーブルを作成・更新するSQLを実行する
10. 処理の開始および終了をログに出力する

対象テーブル：
- `mart_life_daily`
- `mart_life_weekly`（週は月曜始まり）
- `mart_life_monthly`

---

#### 4. 再実行と重複防止

- raw取り込み処理では、同じCSVファイルを再度処理しても
  データが二重登録されない設計とする  
  （例：ファイル名＋CSV行番号をもとに重複を防止）
- stgおよびmartの作成処理は、再実行可能な方式とする  
  （例：対象期間を削除して再作成する、またはテーブルを再生成する）

---

#### 5. エラー発生時の挙動

- raw取り込み中に不正データが検知された場合：
  - ファイル名、行番号、不正内容をログに出力する
  - DBへの変更はロールバックする
  - 以降の処理（stg/mart）は実行しない

- stg/mart作成SQLでエラーが発生した場合：
  - エラー内容をログに出力する
  - 処理を中断する

---

#### 6. ログ出力内容

ログには最低限、以下の情報を出力する。

- 処理対象ファイル名
- CSV読み込み行数
- DB登録件数
- エラー内容（ファイル名・行番号・不正内容）
- 各処理（raw / stg / mart）の開始および終了


## 3. 実装方針
（ディレクトリ構成など）

## 4. テスト方針

